
export const colors =(colorstat)=>{
    if(colorstat==="Danger"){
        return "red_color";
    }
}

export const capitalizeFirstLetter=(word)=> {
    return word.charAt(0).toUpperCase() + word.slice(1);
  }